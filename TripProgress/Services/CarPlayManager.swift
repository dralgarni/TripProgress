import Foundation

// Placeholder for phase two. Keep CarPlay wiring outside the iPhone UI so the
// app can later adopt official CarPlay templates without reshaping trip logic.
final class CarPlayManager {
    static let shared = CarPlayManager()

    private init() {}
}
